<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= product.name %> - Favour Furniture</title>
    <style>
        /* Base Styles */
        :root {
            --primary-color: #8B4513;
            --secondary-color: #2c3e50;
            --accent-color: #D2691E;
            --light-bg: #f8f5f0;
            --white: #ffffff;
            --success: #28a745;
            --error: #dc3545;
            --shadow: 0 4px 12px rgba(0,0,0,0.1);
            --radius: 8px;
        }
        
        * { 
            margin: 0; 
            padding: 0; 
            box-sizing: border-box; 
            -webkit-tap-highlight-color: transparent;
        }
        
        html {
            font-size: 16px;
            scroll-behavior: smooth;
        }
        
        body { 
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            line-height: 1.6; 
            color: #333; 
            background: var(--light-bg);
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        
        /* Container */
        .container { 
            width: 100%;
            max-width: 1200px; 
            margin: 0 auto; 
            padding: 0 16px;
        }
        
        /* Header - Mobile Optimized */
        .site-header { 
            background: var(--white); 
            box-shadow: var(--shadow);
            position: sticky; 
            top: 0; 
            z-index: 1000;
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
        }
        
        .header-content { 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            padding: 12px 0;
            flex-wrap: wrap;
        }
        
        .logo h1 { 
            margin: 0; 
            font-size: 1.25rem;
        }
        
        .logo a { 
            color: var(--primary-color); 
            text-decoration: none; 
            font-weight: 700;
        }
        
        .logo p { 
            font-size: 0.75rem; 
            color: var(--accent-color); 
            margin: 0; 
        }
        
        /* Mobile Navigation */
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--primary-color);
            cursor: pointer;
            padding: 8px;
        }
        
        .main-nav ul { 
            list-style: none; 
            display: flex; 
            gap: 1rem; 
            margin: 0; 
            flex-wrap: wrap;
        }
        
        .main-nav a { 
            text-decoration: none; 
            color: var(--secondary-color); 
            font-weight: 500; 
            padding: 6px 10px; 
            border-radius: 4px;
            font-size: 0.9rem;
            transition: all 0.2s;
        }
        
        .main-nav a:hover, 
        .main-nav a.active { 
            color: var(--primary-color); 
            background: rgba(139, 69, 19, 0.1);
        }
        
        /* Main Content - Mobile First */
        .product-detail { 
            padding: 20px 0 40px;
        }
        
        .product-detail-grid { 
            display: grid; 
            grid-template-columns: 1fr; 
            gap: 30px; 
        }
        
        @media (min-width: 768px) {
            .product-detail-grid {
                grid-template-columns: 1fr 1fr;
                gap: 40px;
            }
        }
        
        .product-images img { 
            width: 100%; 
            height: auto;
            border-radius: var(--radius); 
            box-shadow: var(--shadow);
            aspect-ratio: 4/3;
            object-fit: cover;
        }
        
        .product-info h1 { 
            color: var(--secondary-color); 
            margin-bottom: 8px; 
            font-size: 1.75rem;
            font-weight: 700;
            line-height: 1.3;
        }
        
        .product-type { 
            color: var(--accent-color); 
            font-size: 0.8rem; 
            text-transform: uppercase; 
            margin-bottom: 8px;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        
        .product-price { 
            font-size: 2rem; 
            color: var(--primary-color); 
            font-weight: 700; 
            margin: 16px 0; 
        }
        
        .product-description { 
            margin: 24px 0; 
            padding: 20px;
            background: var(--white);
            border-radius: var(--radius);
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }
        
        .product-description h3 { 
            margin-bottom: 12px; 
            color: var(--secondary-color); 
            border-bottom: 2px solid #f0f0f0;
            padding-bottom: 8px;
            font-size: 1.25rem;
        }
        
        /* Order Form - Mobile Optimized */
        .order-form { 
            background: var(--white); 
            padding: 20px; 
            border-radius: var(--radius); 
            box-shadow: var(--shadow);
            margin-top: 20px;
        }
        
        .order-form h3 { 
            margin-bottom: 20px; 
            color: var(--primary-color); 
            font-size: 1.25rem;
        }
        
        .form-group { 
            margin-bottom: 16px; 
        }
        
        .form-group label { 
            display: block; 
            margin-bottom: 6px; 
            font-weight: 600;
            color: var(--secondary-color);
            font-size: 0.9rem;
        }
        
        .form-group input, 
        .form-group select, 
        .form-group textarea {
            width: 100%;
            padding: 14px;
            border: 2px solid #e0e0e0;
            border-radius: var(--radius);
            font-size: 1rem;
            font-family: inherit;
            transition: border-color 0.2s;
            background: var(--white);
            -webkit-appearance: none;
            appearance: none;
        }
        
        .form-group input:focus, 
        .form-group select:focus, 
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
        }
        
        /* Quantity Selector */
        .quantity-selector {
            display: flex;
            align-items: center;
            gap: 10px;
            max-width: 180px;
        }
        
        .quantity-btn {
            width: 44px;
            height: 44px;
            border: 2px solid var(--primary-color);
            background: var(--white);
            color: var(--primary-color);
            border-radius: 50%;
            font-size: 1.25rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }
        
        .quantity-btn:active {
            transform: scale(0.95);
        }
        
        .quantity-btn:hover {
            background: var(--primary-color);
            color: var(--white); 
        }
        
        .quantity-input {
            width: 60px;
            text-align: center;
            font-size: 1.25rem;
            font-weight: 700;
            border: 2px solid #e0e0e0;
            border-radius: var(--radius);
            padding: 8px;
        }
        
        /* Product Actions */
        .product-actions { 
            display: flex; 
            flex-direction: column;
            gap: 12px; 
            margin-top: 24px;
        }
        
        @media (min-width: 480px) {
            .product-actions {
                flex-direction: row;
                flex-wrap: wrap;
            }
        }
        
        .btn-primary { 
            background: var(--primary-color); 
            color: var(--white); 
            padding: 16px 24px; 
            border-radius: var(--radius); 
            text-decoration: none; 
            border: none; 
            font-size: 1rem; 
            cursor: pointer;
            font-weight: 600;
            transition: all 0.2s;
            flex: 1;
            min-width: 100%;
            touch-action: manipulation;
        }
        
        @media (min-width: 480px) {
            .btn-primary {
                min-width: 180px;
            }
        }
        
        .btn-primary:hover { 
            background: #A0522D;
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(139, 69, 19, 0.2);
        }
        
        .btn-primary:active {
            transform: translateY(0);
        }
        
        .btn-secondary {
            background: var(--secondary-color);
            color: var(--white);
            padding: 16px 24px;
            border-radius: var(--radius);
            text-decoration: none;
            border: none;
            font-size: 1rem;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.2s;
            flex: 1;
            touch-action: manipulation;
        }
        
        .btn-secondary:hover {
            background: #34495e;
            transform: translateY(-2px);
        }
        
        .btn-wishlist { 
            background: transparent; 
            border: 2px solid var(--primary-color); 
            color: var(--primary-color); 
            padding: 16px 24px; 
            border-radius: var(--radius); 
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            transition: all 0.2s;
            flex: 1;
            touch-action: manipulation;
        }
        
        .btn-wishlist:hover { 
            background: var(--primary-color); 
            color: var(--white); 
        }
        
        /* Order Summary */
        .order-summary {
            background: #f9f9f9;
            padding: 20px;
            border-radius: var(--radius);
            margin-top: 20px;
        }
        
        .summary-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }
        
        .summary-row.total {
            border-top: 2px solid var(--primary-color);
            font-size: 1.125rem;
            font-weight: 700;
            color: var(--primary-color);
            padding-top: 15px;
            margin-top: 10px;
        }
        
        /* Admin Contact */
        .admin-contact-section {
            background: #e8f4f8;
            padding: 16px;
            border-radius: var(--radius);
            margin-top: 20px;
            border-left: 4px solid #007bff;
        }
        
        .admin-contact-section h4 {
            color: #0056b3;
            margin-bottom: 12px;
            font-size: 1rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .admin-contact-info {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        
        .admin-contact-item {
            display: flex;
            align-items: flex-start;
            gap: 10px;
            padding: 10px;
            background: var(--white);
            border-radius: 6px;
            font-size: 0.9rem;
        }
        
        .admin-contact-item strong {
            color: #007bff;
            min-width: 60px;
            font-weight: 600;
        }
        
        /* Loading Spinner */
        .spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top-color: var(--white);
            animation: spin 0.8s linear infinite;
            margin-right: 8px;
            vertical-align: middle;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        /* Modal - Mobile Optimized */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 2000;
            overflow-y: auto;
            -webkit-overflow-scrolling: touch;
        }
        
        .modal-content {
            background: var(--white);
            width: 95%;
            max-width: 500px;
            margin: 20px auto;
            padding: 24px;
            border-radius: var(--radius);
            position: relative;
            animation: modalSlideIn 0.3s ease;
        }
        
        @keyframes modalSlideIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .modal-close {
            position: absolute;
            top: 16px;
            right: 16px;
            background: none;
            border: none;
            font-size: 24px;
            cursor: pointer;
            color: #666;
            line-height: 1;
            padding: 4px;
        }
        
        .modal-close:hover {
            color: #333;
        }
        
        /* Alert Messages */
        .alert {
            padding: 12px 16px;
            border-radius: var(--radius);
            margin-bottom: 20px;
            border-left: 4px solid;
            animation: slideIn 0.3s ease;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(-10px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        .alert-success {
            background: #d4edda;
            color: #155724;
            border-left-color: var(--success);
        }
        
        .alert-error {
            background: #f8d7da;
            color: #721c24;
            border-left-color: var(--error);
        }
        
        .alert-info {
            background: #d1ecf1;
            color: #0c5460;
            border-left-color: #17a2b8;
        }
        
        /* Footer */
        .site-footer { 
            background: var(--secondary-color); 
            color: var(--white); 
            padding: 30px 20px; 
            text-align: center; 
            margin-top: 40px; 
        }
        
        /* Utility Classes */
        .hide { display: none !important; }
        .show { display: block !important; }
        .text-center { text-align: center; }
        .mt-20 { margin-top: 20px; }
        .mb-20 { margin-bottom: 20px; }
        
        /* Touch-friendly improvements */
        @media (hover: none) and (pointer: coarse) {
            .btn-primary, .btn-secondary, .btn-wishlist {
                min-height: 50px;
                padding: 16px;
            }
            
            .form-group input, 
            .form-group select, 
            .form-group textarea {
                font-size: 16px; /* Prevents iOS zoom */
                padding: 16px;
            }
        }
        
        /* Dark mode support */
        @media (prefers-color-scheme: dark) {
            body {
                background: #dddddd;
                color: #000000;
            }
            
            .product-description,
            .order-form,
            .admin-contact-item {
                background: #ffffff;
            }
            
            .order-summary {
                background: #ffffff;
            }
        }
    </style>
</head>
<body>
    <header class="site-header">
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <h1><a href="/">Favour Furniture</a></h1>
                    <p>Premium Quality</p>
                </div>
                
                <button class="mobile-menu-btn" id="mobileMenuBtn">☰</button>
                
                <nav class="main-nav" id="mainNav">
                    <ul>
                        <li><a href="/" class="<%= currentPage === 'home' ? 'active' : '' %>">Home</a></li>
                        <li><a href="/products" class="<%= currentPage === 'products' ? 'active' : '' %>">Products</a></li>
                        <li><a href="/orders" class="<%= currentPage === 'orders' ? 'active' : '' %>">My Orders</a></li>
                        <li><a href="/about" class="<%= currentPage === 'about' ? 'active' : '' %>">About</a></li>
                        <li><a href="/contact" class="<%= currentPage === 'contact' ? 'active' : '' %>">Contact</a></li>
                        <% if (typeof user !== 'undefined' && user) { %>
                            <% if (user.role === 'admin') { %>
                                <li><a href="/admin/dashboard">Dashboard</a></li>
                            <% } %>
                            <li><a href="/auth/logout">Logout (<%= user.name %>)</a></li>
                        <% } else { %>
                            <li><a href="/auth/login">Login</a></li>
                            <li><a href="/auth/register">Register</a></li>
                        <% } %>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <main class="product-detail">
        <div class="container">
            <!-- Flash Messages -->
            <% if (typeof success !== 'undefined' && success.length > 0) { %>
                <div class="alert alert-success">
                    <%= success[0] %>
                </div>
            <% } %>
            
            <% if (typeof error !== 'undefined' && error.length > 0) { %>
                <div class="alert alert-error">
                    <%= error[0] %>
                </div>
            <% } %>
            
            <!-- Order Success Alert (shown via JavaScript) -->
            <div id="orderSuccessAlert" class="alert alert-success hide">
                <span id="orderSuccessMessage"></span>
            </div>
            
            <!-- Order Error Alert (shown via JavaScript) -->
            <div id="orderErrorAlert" class="alert alert-error hide">
                <span id="orderErrorMessage"></span>
            </div>
            
            <div class="product-detail-grid">
                <div class="product-images">
                    <% if (product && product.image && product.image !== 'no-image.jpg') { %>
                        <img src="/uploads/<%= product.image %>" alt="<%= product.name %>" loading="lazy">
                    <% } else { %>
                        <img src="/images/placeholder.jpg" alt="No Image Available" loading="lazy">
                    <% } %>
                </div>
                
                <div class="product-info">
                    <p class="product-type"><%= product.type || 'Furniture' %></p>
                    <h1><%= product.name %></h1>
                    <p class="product-price">₦<%= product.price ? product.price.toLocaleString() : '0' %></p>
                    
                    <div class="product-description">
                        <h3>Product Description</h3>
                        <p><%= product.description %></p>
                    </div>
                    
                    <!-- Seller Contact Info -->
                    <div class="admin-contact-section">
                        <h4>📞 Seller Contact Information</h4>
                        <div class="admin-contact-info">
                            <div class="admin-contact-item">
                                <strong>Phone:</strong>
                                <span><%= product.sellerContact?.phone || '+2348012345678' %></span>
                            </div>
                            <div class="admin-contact-item">
                                <strong>Address:</strong>
                                <span><%= product.sellerContact?.address || '123 Main Street, Lagos, Nigeria' %></span>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Order Form -->
                    <div class="order-form">
                        <h3>🛒 Order This Product</h3>
                        
                        <% if (typeof user !== 'undefined' && user) { %>
                            <form id="orderForm" novalidate>
                                <input type="hidden" name="productId" value="<%= product._id %>">
                                
                                <div class="form-group">
                                    <label for="quantity">Quantity:</label>
                                    <div class="quantity-selector">
                                        <button type="button" class="quantity-btn" onclick="decreaseQuantity()" aria-label="Decrease quantity">-</button>
                                        <input type="number" id="quantity" name="quantity" value="1" min="1" max="10" class="quantity-input" required aria-label="Quantity">
                                        <button type="button" class="quantity-btn" onclick="increaseQuantity()" aria-label="Increase quantity">+</button>
                                    </div>
                                </div>

                                <div class="form-group">
                                    <label for="fullName">Full Name *</label>
                                    <input type="text" id="fullName" name="fullName" required 
                                           placeholder="Enter your full name"
                                           value="<%= user && user.name ? user.name : '' %>">
                                </div>

                                <div class="form-group">
                                    <label for="phone">Phone Number *</label>
                                    <input type="tel" id="phone" name="phone" required 
                                           placeholder="08012345678"
                                           value="<%= user && user.phone ? user.phone : '' %>"
                                           pattern="[0-9]{10,11}">
                                    <small style="color: #666; font-size: 0.85rem; display: block; margin-top: 4px;">
                                        We'll call this number for delivery
                                    </small>
                                </div>

                                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px;">
                                    <div class="form-group">
                                        <label for="city">City *</label>
                                        <input type="text" id="city" name="city" required 
                                               placeholder="Your city"
                                               value="<%= user && user.city ? user.city : 'Lagos' %>">
                                    </div>
                                    
                                    <div class="form-group">
                                        <label for="state">State *</label>
                                        <input type="text" id="state" name="state" required 
                                               placeholder="Your state"
                                               value="<%= user && user.state ? user.state : 'Lagos' %>">
                                    </div>
                                </div>
                                
                                <div class="order-summary">
                                    <div class="summary-row">
                                        <span>Product Price:</span>
                                        <span>₦<span id="productPrice"><%= product.price %></span></span>
                                    </div>
                                    <div class="summary-row">
                                        <span>Quantity:</span>
                                        <span id="quantityDisplay">1</span>
                                    </div>
                                    <div class="summary-row">
                                        <span>Shipping Fee:</span>
                                        <span>₦2,000</span>
                                    </div>
                                    <div class="summary-row total">
                                        <span>Total Amount:</span>
                                        <span>₦<span id="totalAmount"><%= product.price + 2000 %></span></span>
                                    </div>
                                </div>
                                
                                <div class="product-actions">
                                    <button type="submit" class="btn-primary" id="submitBtn">
                                        <span id="submitText">Place Order Now</span>
                                        <span id="submitSpinner" class="spinner hide"></span>
                                    </button>
                                    <button type="button" class="btn-wishlist" onclick="likeProduct('<%= product._id %>')">
                                        ♡ Like (<span id="like-count"><%= product.likes || 0 %></span>)
                                    </button>
                                </div>
                            </form>
                        <% } else { %>
                            <div class="login-warning">
                                <h4>🔒 Login Required</h4>
                                <p>Please <a href="/auth/login">login</a> to place an order.</p>
                                <p class="mt-20"><a href="/auth/register" class="btn-secondary" style="display: inline-block; padding: 12px 24px;">Create Account</a></p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Simple Success Modal -->
    <div id="successModal" class="modal">
        <div class="modal-content">
            <button class="modal-close" onclick="closeModal()">&times;</button>
            <div style="text-align: center; padding: 20px;">
                <div style="font-size: 60px; color: var(--success); margin-bottom: 20px;">✓</div>
                <h2 style="margin-bottom: 15px; color: var(--primary-color);">Order Successful!</h2>
                <p id="successMessage" style="margin-bottom: 30px; font-size: 1.1rem;"></p>
                <div style="display: flex; flex-direction: column; gap: 10px; margin-top: 30px;">
                    <a href="#" class="btn-primary">Seller Contact: +2348012345678</a>
                    <a href="/products" class="btn-secondary">Continue Shopping</a>
                </div>
            </div>
        </div>
    </div>

    <footer class="site-footer">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %> Favour Furniture Int'l Company Ltd</p>
            <p style="margin-top: 10px; font-size: 0.9rem; opacity: 0.8;">Quality Furniture for Every Home</p>
        </div>
    </footer>

    <script>
        // Mobile menu toggle
        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
        const mainNav = document.getElementById('mainNav');
        
        if (mobileMenuBtn && mainNav) {
            mobileMenuBtn.addEventListener('click', () => {
                mainNav.classList.toggle('show');
            });
            
            // Close menu when clicking outside
            document.addEventListener('click', (e) => {
                if (!mainNav.contains(e.target) && !mobileMenuBtn.contains(e.target)) {
                    mainNav.classList.remove('show');
                }
            });
            
            // Close menu on window resize
            window.addEventListener('resize', () => {
                if (window.innerWidth > 768) {
                    mainNav.classList.remove('show');
                }
            });
        }
        
        // Quantity Controls
        const quantityInput = document.getElementById('quantity');
        const quantityDisplay = document.getElementById('quantityDisplay');
        const productPrice = parseFloat(document.getElementById('productPrice').textContent);
        const totalAmountElement = document.getElementById('totalAmount');
        const shippingFee = 2000;

        function updateTotal() {
            const quantity = parseInt(quantityInput.value) || 1;
            const subtotal = productPrice * quantity;
            const total = subtotal + shippingFee;
            totalAmountElement.textContent = total.toLocaleString();
            quantityDisplay.textContent = quantity;
        }

        function increaseQuantity() {
            let value = parseInt(quantityInput.value) || 1;
            if (value < 10) {
                quantityInput.value = value + 1;
                updateTotal();
            }
        }

        function decreaseQuantity() {
            let value = parseInt(quantityInput.value) || 1;
            if (value > 1) {
                quantityInput.value = value - 1;
                updateTotal(); 
            }
        }

        quantityInput.addEventListener('input', function() {
            let value = parseInt(this.value) || 1;
            if (value < 1) value = 1;
            if (value > 10) value = 10;
            this.value = value;
            updateTotal();
        });

        // Initialize total
        updateTotal();

        // Order Form Submission - SIMPLIFIED
        document.getElementById('orderForm')?.addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const submitBtn = document.getElementById('submitBtn');
            const submitText = document.getElementById('submitText');
            const submitSpinner = document.getElementById('submitSpinner');
            
            // Show loading state
            submitBtn.disabled = true;
            submitText.textContent = 'Processing...';
            submitSpinner.classList.remove('hide');
            
            // Hide any previous alerts
            document.getElementById('orderSuccessAlert').classList.add('hide');
            document.getElementById('orderErrorAlert').classList.add('hide');
            
            try {
                // Collect form data
                const formData = {
                    productId: this.productId?.value,
                    quantity: parseInt(this.quantity?.value) || 1,
                    fullName: this.fullName?.value?.trim(),
                    phone: this.phone?.value?.trim(),
                    city: this.city?.value?.trim(),
                    state: this.state?.value?.trim()
                };
                
                // Basic validation
                if (!formData.phone || formData.phone.length < 10) {
                    throw new Error('Please enter a valid phone number (10+ digits)');
                }
                
                // Send request
                const response = await fetch('/orders', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(formData)
                });
                
                // Parse response
                let data;
                try {
                    data = await response.json();
                } catch (jsonError) {
                    throw new Error('Server returned invalid response');
                }
                
                // Handle response
                if (data.success) {
                    // Show success message
                    document.getElementById('successMessage').textContent = data.message;
                    document.getElementById('successModal').style.display = 'block';
                    
                    // Reset form
                    this.reset();
                    quantityInput.value = 1;
                    updateTotal();
                    
                    // Hide form after successful order (optional)
                    // setTimeout(() => {
                    //     window.location.href = '/orders';
                    // }, 3000);
                    
                } else {
                    // Show error message
                    document.getElementById('orderErrorMessage').textContent = data.message;
                    document.getElementById('orderErrorAlert').classList.remove('hide');
                }
                
            } catch (error) {
                console.error('Order error:', error);
                document.getElementById('orderErrorMessage').textContent = error.message;
                document.getElementById('orderErrorAlert').classList.remove('hide');
                
                // Scroll to error
                document.getElementById('orderErrorAlert').scrollIntoView({ 
                    behavior: 'smooth',
                    block: 'center'
                });
                
            } finally {
                // Reset button state
                submitBtn.disabled = false;
                submitText.textContent = 'Place Order Now';
                submitSpinner.classList.add('hide');
            }
        });

        // Like Product Function
        async function likeProduct(productId) {
            try {
                const response = await fetch(`/api/products/${productId}/like`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' }
                });
                
                const data = await response.json();
                
                if (data.success) {
                    document.getElementById('like-count').textContent = data.likes;
                    // Show a quick success message
                    const likeBtn = document.querySelector('.btn-wishlist');
                    const originalText = likeBtn.textContent;
                    likeBtn.textContent = '♥ Liked!';
                    setTimeout(() => {
                        likeBtn.textContent = originalText;
                    }, 2000);
                }
            } catch (error) {
                console.error('Error:', error);
            }
        }

        // Modal Functions
        function closeModal() {
            document.getElementById('successModal').style.display = 'none';
        }

        // Close modal when clicking outside
        window.onclick = function(event) {
            const modal = document.getElementById('successModal');
            if (event.target === modal) {
                closeModal();
            }
        }

        // Prevent form resubmission on page refresh
        if (window.history.replaceState) {
            window.history.replaceState(null, null, window.location.href);
        }
        
        // Store product name for modal
        window.__CURRENT_PRODUCT_NAME = '<%= product.name %>';
    </script>
</body>
</html>